# 📂 Проект: Фильтрация данных по ИНН

## 📌 Описание
Минипроект предназначен для фильтрации данных о компаниях по списку ИНН.  

**Исходные данные:**
- `inn.xls` — Excel-файл с информацией о компаниях, включая колонки `head_inn` и `income,RUB`.  
- `necessary_inn.txt` — текстовый файл с перечнем нужных ИНН (без заголовка).  

**Цель проекта:**
- Отобрать из Excel только записи с ИНН, указанными в текстовом файле.  
- Сохранить результат в CSV (`selected_inn.csv`).  
- Рассчитать и вывести сумму по колонке `income,RUB` для отобранных компаний.

---

## 🛠 Технологии
- Python 3.x  
- Pandas  
- OpenPyXL (для работы с Excel)

---

## 🚀 Запуск проекта
1. Склонируйте репозиторий или скачайте файлы проекта.  
2. Установите зависимости:  
   ```bash
   pip install pandas openpyxl
Запустите скрипт:

bash
Копировать код
python filter_inn.py
После выполнения:

CSV-файл с отобранными данными будет сохранён как selected_inn.csv.

В консоли появится сумма колонки income,RUB для выбранных записей.

📊 Как работает скрипт
Чтение исходных данных

python
Копировать код
df = pd.read_excel("inn.xls")
necessary_inn = pd.read_csv("necessary_inn.txt", header=None)[0].tolist()
Фильтрация по ИНН

python
Копировать код
filtered_df = df[df["head_inn"].isin(necessary_inn)]
Сохранение результата в CSV

python
Копировать код
filtered_df.to_csv("selected_inn.csv", index=False)
Вычисление суммы по колонке income,RUB

python
Копировать код
income_sum = filtered_df["income,RUB"].sum()
print("Сумма колонки income,RUB в отобранных данных:", income_sum)
✅ Итог
Скрипт позволяет:

Быстро отфильтровать данные по списку ИНН.

Сохранить результат в отдельный CSV-файл.

Проверить корректность фильтрации через сумму доходов (income,RUB).
